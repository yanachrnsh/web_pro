// let arr = ['HTML', 'JavaScript', 'CSS'];

// let copyForSort = (arr) => arr.slice().sort();

// console.log(arr);
// console.log(copyForSort(arr));

const obj1 = {
	name: 'Yana',
	age: 20,
};

for (let key in obj1) {
	console.log(key);
	console.log(obj1[key]);
}
